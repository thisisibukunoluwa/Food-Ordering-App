class APIData {
  static String fetchRestaurants() => 'assets/json/response.json';

  // filter through a list of json objects to return the appropriate json data for the specified restaurant
  // its like when we construct a URI to a resource and it returns the reosource we want 
}

//one endpoint tht gives list of restaurants

// it doesnt have to be a different endpoint 

// on endpoint that gives individual restaurant data 

